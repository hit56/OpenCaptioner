# 复制为 smtp.env 后填写真实密码：
#   cp smtp.env.example smtp.env
# smtp.env 已 gitignore，勿提交。
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SSL=1
SMTP_USER=noreply@example.com
SMTP_PASSWORD=
SMTP_FROM=noreply@example.com
# 无法直连 SMTP 时走 HTTP CONNECT 代理
# SMTP_HTTP_PROXY=http://user:pass@proxy-host:3120
